name: Apply MQS CN Theme Parity Once

on:
  push:
    branches:
      - feature/mqs-cn-theme-parity

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: python scripts/tmp-mqs-cn-theme-parity.py
      - run: pnpm exec prettier --write src/pages/MqsPrototype.tsx src/index.css tests/mqs-prototype.spec.ts
      - run: pnpm typecheck
      - run: pnpm exec playwright install chromium
      - run: pnpm exec playwright test tests/mqs-prototype.spec.ts --project=chromium
      - name: Commit feature and remove temporary files
        run: |
          git config user.name "Russfranky"
          git config user.email "90220424+russfranky@users.noreply.github.com"
          git rm scripts/tmp-mqs-cn-theme-parity.py .github/workflows/apply-mqs-cn-theme-parity-once.yml
          git add src/pages/MqsPrototype.tsx src/index.css tests/mqs-prototype.spec.ts
          git commit -m "fix: match MQS palette to /cn"
          git push origin HEAD:feature/mqs-cn-theme-parity
